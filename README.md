# 我們的小天地

情侶用的小工具：紀念日天數、雙方大頭貼、想吃清單、Google 地圖。
單一 HTML 檔，沒有任何相依套件。

資料同步由一支 Google Apps Script Web App 負責。
同步位址與密碼**不寫在原始碼裡**，改由網址片段帶入：

```
https://<帳號>.github.io/couple-app/#u=<exec 網址>&p=<密碼>
```

`#` 後面的內容不會送到伺服器，也不在這個 repo 裡。
第一次開啟時會存進瀏覽器本機，接著網址列會自動清乾淨。
